; A single, deterministic companion.  The custom NPC section points at the
; companion profile instead of one of the simulator's random profiles.
[companion_fixed]:sim_default_stalker_0
character_profile = companion_fixed
